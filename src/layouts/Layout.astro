---
import Header from "src/components/Header.astro";
import Footer from "src/components/Footer.astro"

const { ...customMeta } = Astro.props;
const meta = {
	title: "Astro",
	description: "Build fast websites, faster. Astro is a new kind of static site builder for the modern web. Powerful developer experience meets lightweight output.",
	image: "https://astro.build/social.png",
	type: "website",
	siteName: 'Astro',
	twitterUser: '@astrodotbuild',
	...customMeta,
};
const currentPage = Astro.canonicalURL.pathname;
// https://docs.astro.build/en/reference/api-reference/#astrocanonicalurl
---

<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8" />
	<meta name="viewport" content="width=device-width" />
	<link rel="icon" type="image/x-icon" href="/favicon.ico" />
	<link rel="canonical" href={`https://astro-init.vercel.app${currentPage}`} />
	<link rel="preload" href="/fonts/inter-var.woff2" as="font" type="font/woff2" crossorigin="anonymous" />

	<title>{meta.title}</title>
	<meta content={meta.description} name="description" />

	<meta property="og:url" content={`https://astro-init.vercel.app${currentPage}`} />
	<meta property="og:type" content={meta.type} />
	<meta property="og:site_name" content={meta.siteName} />
	<meta property="og:description" content={meta.description} />
	<meta property="og:title" content={meta.title} />
	<meta property="og:image" content={meta.image} />

	<meta name="twitter:card" content="summary_large_image" />
	<meta name="twitter:site" content={meta.twitterUser} />
	<meta name="twitter:title" content={meta.title} />
	<meta name="twitter:description" content={meta.description} />
	<meta name="twitter:image" content={meta.image} />
</head>

<body class="flex flex-col min-h-screen text-[#111111] font-sans">
	<Header />
	<main class="flex-grow w-full max-w-4xl px-4 mx-auto my-10 antialiased sm:px-6 md:my-12 lg:px-8">
		<slot />
	</main>
	<Footer />
</body>

</html>

<style>
	@font-face {
		font-family: "Inter";
		font-style: normal;
		font-weight: 100 900;
		font-display: optional;
		src: url(/fonts/inter-var.woff2) format("woff2");
	}
</style>